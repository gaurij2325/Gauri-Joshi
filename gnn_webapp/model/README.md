# Model Files

This folder contains artifacts related to the trained GNN model.

## Excluded Files
- Trained model file (`.pth`)
- Serialized graph data (`.pkl`)

## Usage
The model and graph data are loaded at runtime by the Flask application for inference purposes.

